---
const features = [
	{
		id: 'emr',
		title: 'EMR',
		copy: 'Fast prescriptions, specialty forms, clickable notes, and a face sheet that summarises the patient.',
		href: '#emr',
	},
	{
		id: 'scheduling',
		title: 'Scheduling',
		copy: 'Walk-ins, timed slots, teleconsults, waiting-room status, and reminders that cut no-shows across sites.',
		href: '#scheduling',
	},
	{
		id: 'billing',
		title: 'Billing',
		copy: 'One-click invoices, part payments, tax rules, and a till that balances cash, card, and online.',
		href: '#billing',
	},
	{
		id: 'cura',
		title: 'Cura AI',
		copy: 'AI EMR copilot for decisions, safety alerts, claims, and supply — advisory, doctor still in charge.',
		href: '/curapilot',
		featured: true,
	},
	{
		id: 'inventory',
		title: 'Inventory',
		copy: 'Stock, barcodes, reorders, and transfers — with AI-assisted reorder levels.',
		href: '#inventory',
	},
	{
		id: 'pharmacy',
		title: 'Pharmacy',
		copy: 'Prescriptions flow to dispensing; expiry flags at sale; refill reminders go out automatically.',
		href: '#pharmacy',
	},
	{
		id: 'lab',
		title: 'Lab',
		copy: 'Orders from the EMR, abnormal values on the report, results graphed beside the visit.',
		href: '#lab',
	},
	{
		id: 'reports',
		title: 'Reports',
		copy: '100+ dashboards for clinical, inventory, and finance — exportable and access-controlled.',
		href: '#reports',
	},
	{
		id: 'engagement',
		title: 'Engagement',
		copy: 'Portal, WhatsApp, SMS, and email for booking, records, and feedback — under your clinic brand.',
		href: '#engagement',
	},
	{
		id: 'multi-location',
		title: 'Multi-location',
		copy: 'One patient record across owned and franchise sites, with SOPs, KPIs, and a chain dashboard.',
		href: '#multi-location',
	},
];
---

<section class="grid-sec">
	<div class="panel">
		<p class="eyebrow">Modules</p>
		<h2>Modules clinics use every day</h2>
		<p class="lede">
			Ten parts of EasyClinic doctor practice software — from the consult to the till. Cura AI ties
			the EMR to operations.
		</p>
		<ul class="cards" data-stagger>
			{
				features.map((item) => (
					<li id={item.id}>
						{item.featured ? (
							<a class="card featured" href={item.href}>
								<h3>{item.title}</h3>
								<p>{item.copy}</p>
								<span class="more">Explore Cura AI</span>
							</a>
						) : (
							<article class="card">
								<h3>{item.title}</h3>
								<p>{item.copy}</p>
							</article>
						)}
					</li>
				))
			}
		</ul>
	</div>
</section>
<style>
	.grid-sec {
		background: var(--brand-wash);
	}
	.panel {
		background: var(--surface);
		border-radius: 24px 24px 0 0;
		padding: 5.5rem 1.5rem 3rem;
		color: var(--ink);
		font-size: 16px;
		line-height: 1.5;
	}
	.eyebrow {
		margin: 0;
		color: var(--brand-blue);
		font-size: 0.875rem;
		font-weight: 600;
		letter-spacing: 0.08em;
		text-align: center;
		text-transform: uppercase;
	}
	h2 {
		max-width: 16ch;
		margin: 0.85rem auto 0;
		color: var(--navy);
		font-size: clamp(1.875rem, 3.4vw, 3rem);
		font-weight: 700;
		line-height: 1.1;
		letter-spacing: -0.02em;
		text-align: center;
	}
	.lede {
		max-width: 52ch;
		margin: 1.15rem auto 0;
		color: var(--muted);
		font-size: 1.125rem;
		text-align: center;
	}
	.cards {
		display: grid;
		grid-template-columns: repeat(3, minmax(0, 1fr));
		gap: 1.15rem;
		max-width: var(--page-max);
		margin: 3rem auto 0;
		padding: 0;
		list-style: none;
	}
	.cards li {
		scroll-margin-top: 6rem;
	}
	.card {
		display: flex;
		flex-direction: column;
		height: 100%;
		border: 1px solid var(--border);
		border-radius: 20px;
		padding: 1.6rem 1.5rem 1.35rem;
		background: var(--surface-tint);
		color: inherit;
		text-decoration: none;
	}
	.card:hover {
		border-color: var(--brand-blue);
	}
	.card.featured {
		border-color: transparent;
		background: var(--navy);
	}
	.card.featured h3,
	.card.featured .more {
		color: var(--surface);
	}
	.card.featured p {
		color: rgba(255, 255, 255, 0.72);
	}
	h3 {
		margin: 0;
		color: var(--navy);
		font-size: 1.25rem;
		font-weight: 700;
	}
	.card p {
		margin: 0.75rem 0 1.25rem;
		color: var(--muted);
		flex: 1;
	}
	.more {
		color: var(--brand-blue);
		font-size: 0.875rem;
		font-weight: 600;
	}

	@media (max-width: 1080px) {
		.panel {
			padding: 4rem 1.5rem 2.5rem;
		}
		.cards {
			grid-template-columns: repeat(2, minmax(0, 1fr));
		}
	}
	@media (max-width: 640px) {
		.panel {
			padding: 3rem 1rem 2rem;
		}
		.cards {
			grid-template-columns: 1fr;
		}
	}
</style>
