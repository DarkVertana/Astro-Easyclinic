---
import SectionHead from '../SectionHead.astro';
import type { VsPageData } from './types';

interface Props {
	data: VsPageData['table'];
}

const { data } = Astro.props;
---

<section class="diff">
	<div class="inner">
		<SectionHead title={data.title} lede={data.lede} />
		<div class="table-wrap" role="region" aria-label="Comparison table" tabindex="0">
			<table>
				<thead>
					<tr>
						{data.headers.map((h) => <th scope="col">{h}</th>)}
					</tr>
				</thead>
				<tbody>
					{
						data.rows.map((row) => (
							<tr>
								{row.map((cell, i) => (i === 0 ? <th scope="row">{cell}</th> : <td>{cell}</td>))}
							</tr>
						))
					}
				</tbody>
			</table>
		</div>
	</div>
</section>

<style>
	.diff {
		background: var(--brand-wash);
		padding: 5rem 1.5rem;
	}
	.inner {
		max-width: var(--page-max);
		margin: 0 auto;
	}
	.table-wrap {
		overflow-x: auto;
		margin: 2.75rem auto 0;
		border: 1px solid rgba(10, 37, 64, 10%);
		border-radius: 18px;
		background: #fff;
		-webkit-overflow-scrolling: touch;
	}
	table {
		width: 100%;
		min-width: 560px;
		border-collapse: collapse;
		font-size: 0.9375rem;
	}
	th,
	td {
		padding: 0.95rem 1rem;
		border-bottom: 1px solid rgba(10, 37, 64, 8%);
		text-align: left;
		vertical-align: top;
		color: var(--muted);
		line-height: 1.45;
	}
	thead th {
		background: var(--brand-wash);
		color: var(--navy);
		font-weight: 700;
		white-space: nowrap;
	}
	tbody th {
		color: var(--navy);
		font-weight: 650;
		width: 28%;
	}
	tbody tr:last-child th,
	tbody tr:last-child td {
		border-bottom: 0;
	}
	tbody td:nth-child(2) {
		color: var(--navy);
		font-weight: 600;
	}
</style>
